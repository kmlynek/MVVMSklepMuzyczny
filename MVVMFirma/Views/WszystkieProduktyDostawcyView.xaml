<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieProduktyDostawcyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ProduktDostawcaID}"/>
                <DataGridTextColumn Header="Nazwa Produktu" Binding="{Binding ProduktyNazwa}"/>
                <DataGridTextColumn Header="Cena" Binding="{Binding ProduktyCena}"/>
                <DataGridTextColumn Header="Nazwa Dostawcy" Binding="{Binding DostawcyNazwa}"/>
                <DataGridTextColumn Header="Dostawca Kontakt" Binding="{Binding DostawcyKontakt}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
